import styled from "styled-components";
import image from '../../ui/img/miles.jpg';

const Wrapper = styled.div`
  width: 100%;
  height: 100vh;
  background-image: url(${image});
  background-size: cover;
`;

export default { Wrapper };